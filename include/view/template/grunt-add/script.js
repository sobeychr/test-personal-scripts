(function (doc, $, undefined) {
    'use strict';

    var {mo} = (function() {

        var _configs = {
        };

        var init = function() {
        };

        return {
            init:  init,
        };
    })();

    $(function() {
        {mo}.init();
    });
}(document, jQuery));
