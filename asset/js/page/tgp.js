(function (doc, $, undefined) {
    'use strict';

    var tgp = (function() {

        var _configs = {
        };

        var init = function() {
        };

        return {
            init:  init,
        };
    })();

    $(function() {
        tgp.init();
    });
}(document, jQuery));
