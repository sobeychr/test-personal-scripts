(function (doc, $, undefined) {
    'use strict';

    var upload = (function() {

        var _configs = {
        };

        var init = function() {
        };

        return {
            init:  init,
        };
    })();

    $(function() {
        upload.init();
    });
}(document, jQuery));
