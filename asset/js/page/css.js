(function (doc, $, undefined) {
    'use strict';

    var css = (function() {

        var _configs = {
        };

        var init = function() {
        };

        return {
            init:  init,
        };
    })();

    $(function() {
        css.init();
    });
}(document, jQuery));
