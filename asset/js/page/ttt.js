(function (doc, $, undefined) {
    'use strict';

    var ttt = (function() {

        var _configs = {
        };

        var init = function() {
        };

        return {
            init:  init,
        };
    })();

    $(function() {
        ttt.init();
    });
}(document, jQuery));
